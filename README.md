# Product Info H5

This demo provides a simple product information H5 page with a minimal backend.

## Requirements
- Node.js (v16 or later)

## Usage
```
node server.js
```
Then open <http://localhost:3000> in your browser.

Products and users are stored in the `data/` folder as JSON files.
